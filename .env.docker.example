# Copy to .env in the project root (or merge with your existing .env) for docker compose.

MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=srusystem
MYSQL_USER=srusystem
MYSQL_PASSWORD=srusystem

APP_PORT=8000
APP_ENV=prod
APP_DEBUG=0
APP_SECRET=ChangeMeInProduction

# Docker Compose only: seed 5 demo products when the catalog is empty
# SEED_DEMO_PRODUCTS=1

# Railway: see .env.railway.example (migrations run automatically on deploy)
